A simple 3D rendering engine with Python

for use the engine install the all library's

pip install git+https://github.com/mcfletch/pyopengl.git

pip install pygame

pip install json

pip install math
